<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Courses</title>
    <%- include('../partials/main') %>
  </head>
  <% if(typeof user === 'undefined') user = {} %>
  <body>
    <div class="app container bg-white border-top border-4 border-success">
      <%- include('../partials/header') %>
      <div class="mx-5 px-5">
        <h3 class="text-center mt-3">User's Profile</h3>
        <p>
          Username:
          <span class="text-success fw-medium fs-5"><%= usr.username %></span>
        </p>
        <p>
          Name:
          <span class="text-success fw-medium fs-5"><%= usr.name %></span>
        </p>
        <p>
          Role:
          <span class="text-success fw-medium fs-5"><%= usr.role %></span>
        </p>
        <p>
          Created At:
          <span class="text-success fw-medium fs-5"
            ><%= usr.createdAt.toLocaleString() %></span
          >
        </p>
        <h3 class="text-center border-top pt-4 mt-2">My Courses</h3>
        <% if(courses.length !== 0) { %>
        <ul class="list-group list-group-flush my-3">
          <% for(let i = 0; i < courses.length; i++) { %>
          <li class="d-flex list-group-item">
            <a
              class="col-10 text-decoration-none"
              href="/course/<%= courses[i].slug %>"
            >
              <%= courses[i].c_id %>[<%= courses[i].faculty %>] <%=
              courses[i].name[0] %> - <%= courses[i].name[1] %>
            </a>
          </li>
          <% } %>
        </ul>

        <% } else { %>
        <h6 class="text-center text-success">No courses enrolled yet!</h6>
        <%} %>
      </div>
      <%- include('../partials/footer') %>
    </div>
  </body>
</html>
